/**
 * setupTests.js — Jest global setup
 *
 * Imports @testing-library/jest-dom so that custom matchers like
 * toBeInTheDocument(), toHaveTextContent(), etc. are available in all test files.
 */

import "@testing-library/jest-dom";
